Ideas
Themes
	Culinary matching (leftovers club)
	Alumni only plattform (ULoveToSeeIt)
	.
	Fields & Futures - Rural, intentional dating 

Features
	Reputation score - displayed on profile but not filtered
	- User warning when low rep score
	Compatibility score (through interest tags) - yes
	Profile view count (possible viewer ID and timestamp) - maybe
	Active / Inactive banners based on last log-in / activity - toggle in app - yes
	.
	Profile completeness based off information filled in (picture, bio, 70% complete etc) -yes
	Soft delete - Instead of removing offending users from the database a value that identifies if they are permitted to use the service is  set to false -yes
	.
	Ai Generated compatibility score - maybe
	"Why you'd be a good fit" generated caption before matching -yes
	.
	MiniGames in profile prior to matching - "2 truths and a lie" interactive game, etc
	Interest heatmap/ correlation matrix - maybe
	.
	Read receipts for messages - yes
	Icebreaker suggestion engine- maybe
	Reputation score per profile - yep
	Overview dashboard per user - maybe
	Overview dashboard for admin - yes
	.
	Dark mode - yes
	.
	Swipe / swipe alternative UI
		Instagram reel style centre window
		Semi-transparent images on the sides
		Scroll through User created "windows/views"
		EXAMPLE - Landing page, Bio, hobbies, Interests, Goals, etc
		Must scroll through x amount of windows / timer based before approving or disapproving
		.
	Real time moderation que for reported users - 1 click banning - yes
	Report counter for severity - 1 report means in que, 2+ report means top of que - yes
	Enhanced recommendation logic 
	
User table set-up

- user_id (UNIQUE)
- username (Actual name)
- password_hash
- role (user / admin)
- is_active (boolean for soft delete)
- created_at
- last_login_at
- is_banned (boolean)
- Personal folder address/ID
	- BIO
	- PICS
	- Layout of their profile - windows

USER INTRESTS TABLE
	- user_id (UNIQUE)
	- user_type - Sporty, gamer, adventurer
	- user_Preferred_Type - ALL, outdoors/outgoing, Quiet/easygoing, etc
	- user_Interest1 - First preference interest stored as a string (E.G FISHING)
	- user_Interest2 - Second preference interest stored as a string (E.G ROCKCLIMBING)
	- user_Interest3 - Third preference interest stored as a string (E.G GAMING)

Preferences table
- user_id 
- age
- sex
- location
- reputation_score - thresholds- 50%, 80%+

Messages table

Matches table
